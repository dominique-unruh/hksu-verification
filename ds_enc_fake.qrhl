include "variables.qrhl".
include "Adv_DS_enc.qrhl".

# DS indistiguishability game 1, for punc_enc, adversary A
program ds_enc_fake := {
  (pk,sk) <$ keygen ();
  cstar <$ fakeenc () pk;
  in_pk <- pk;
  in_cstar <- cstar;
  call Adv_DS_enc;
}.
