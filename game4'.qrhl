# Autogenerated file (by replacing Adv_DS_encT -> Adv_DS_encT', etc. etc. in game4.qrhl)

include "Adv_DS_encT'.qrhl".
include "queryGPuncturedS.qrhl".

program game4' := {
  Find <- False;
  G <$ uniform UNIV;
  (pk,sk) <$ keygen ();
  mstar <$ uniform (msg_space ());
  rstar <$ uniform UNIV;
  cstar <- encr () pk mstar rstar;
  S <- {mstar};
  in_pk <- pk;
  in_cstar <- cstar;
  call Adv_DS_encT'(queryGPuncturedS);
}.
# return Find
